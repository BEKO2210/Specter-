# Specter — Abhängigkeiten (Voll-Installation inkl. optionalem KI-Lauf).
#
# Der defensive Offline-Kern (14 Analyzer, Report, Scope-Policy) braucht NUR
# PyYAML und rich. Das Paket 'anthropic' wird ausschließlich für den optionalen,
# KI-gesteuerten autonomen Lauf benötigt (nur mit ANTHROPIC_API_KEY).
#
# Nur Offline-Kern ohne KI:  pip install -r requirements-core.txt
# Modernes Packaging/Extras:  pip install .   bzw.   pip install .[ai]

PyYAML>=6.0             # Scope- und Konfigurationsdateien
rich>=13.0              # Lesbare Terminal-Ausgabe (Reports, Logs)
anthropic>=0.40.0       # OPTIONAL: nur für den KI-gesteuerten Lauf (Tool-Calling)
